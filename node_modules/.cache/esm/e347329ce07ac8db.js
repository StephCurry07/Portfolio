let fetch;_e07‍.w("node-fetch",[["default",["fetch"],function(v){fetch=v}]]);const http = require('http');
const fs = require('fs');
const path = require('path');


const server = http.createServer(async (req, res) => {
    // Set the content type to HTML
    res.setHeader('Content-Type', 'text/html');

    // Read the HTML file
    const htmlFilePath = path.join(__dirname, 'test.html');
    const htmlStream = fs.createReadStream(htmlFilePath);

    // Pipe the HTML file to the response
    htmlStream.pipe(res);
});

const PORT = process.env.PORT || 3000;
server.listen(PORT, () => {
    console.log(`Server running on http://localhost:${PORT}`);
});
